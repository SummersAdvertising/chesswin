<% if notice
		%>		
		<p class="warning"><%= notice %></p>		
		<%
end %>

<%= form_for( @user, :url => {:controller => :users, :action => :get_permission} ) do | f |  %>
<%= hidden_field_tag 'redirect',  session[:redirect]  %>

    <h2><img src="<%= image_path("man.png") %>" width="16" height="16" /> 使用者登入</h2>
    <table width="100%" border="0" cellspacing="0" cellpadding="0">
      <tr>
        <td colspan="2" align="left">帳號<br>
          <%= f.text_field :username%></td>
      </tr>
      <tr>
        <td colspan="2" align="left">密碼<br>
          <%= f.password_field :password %></td>
      </tr>
      <!--tr>
        <td width="117" align="left">是否記住密碼？
          </td>
        <td width="283" align="left"><input name="" type="checkbox" value="" style="width:25px"/></td>
      </tr-->
    </table>
    <div class="button">
     <img src="<%= image_path("login.png") %>"><%= f.submit "登入" %>
      </div>
      <!--div class="button"><a href="forget.html"><img src="images/psw.png">忘記密碼</a></div-->
      
	
<% end %>
